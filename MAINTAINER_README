# Building



#Autotools

The autotools based build is deprecated.

### To create a distributable source tree, with a configure script and everything, run these commands

aclocal
autoconf
automake --add-missing
